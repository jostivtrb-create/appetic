# Dependencias
node_modules
.pnp
.pnp.js

# Build
dist
dist-ssr
*.local

# Variables de entorno (claves) — NUNCA subir
.env
.env.*
!.env.example

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
logs
*.log

# Editor / sistema
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
Thumbs.db

# Firebase local
.firebase
firebase-debug.log

# Llave de servicio (NUNCA subir — da acceso total al proyecto)
scripts/serviceAccount.json
serviceAccount*.json

# Respaldos automáticos de los seeds y copias de recuperación (datos de negocio, no van a git)
scripts/.seed-backups/
scripts/_recovery-*.json
scripts/_backup-*.json
